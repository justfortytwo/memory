{
  "name": "memory",
  "version": "0.1.0",
  "description": "Standalone semantic-memory MCP server (SQLite + sqlite-vec + Ollama embedder). Stores and recalls memories by meaning; persona-agnostic."
}
